# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
.env
.venv
env/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Project specific
*.wav
output/
output_*/
basic_audio.wav
full_conversation.wav
context_audio.wav
TTS_MEASURED_PERFORMANCE.md

# Model files
*.pt
*.ckpt
*.safetensors

# Performance work — local working docs and generated eval artifacts.
# The harness CODE (perf_eval/*.py) is tracked; its outputs and the vendored
# perceval eval package are not. The analysis + diary stay local (commit
# messages must restate rationale standalone).
PERFORMANCE_ANALYSIS.md
PERFORMANCE_PROGRESS.md
/perceval/
perf_eval/baseline/
perf_eval/runs/
perf_eval/reports/
perf_eval/out/
perf_eval/*.log

# Voice prompt assets (registered at runtime); keep the README only.
deploy/prompts/*.wav
deploy/prompts/*.txt

# Transient build / profile / push logs from container work.
deploy/*.log

# Claude Code artifacts (anywhere in the tree)
.claude/
CLAUDE.md
CLAUDE.local.md
.claude.json
.claude.local.json
